@model SkillLevelViewModel

@{
    ViewData["Title"] = "Редактиране на ниво на умение";
}

<div class="admin-content-header">
    <h1 class="admin-title">
        <i class="fas fa-edit me-2"></i>
        Редактиране на ниво на умение
    </h1>
</div>

<div class="admin-form-wrapper">
    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger">@TempData["ErrorMessage"]</div>
    }

    <form asp-action="Edit" method="post" class="admin-form">
        @Html.AntiForgeryToken()

        <input type="hidden" asp-for="SkillLevelId" />

        <div class="row">
            <div class="col-md-8">
                <div class="form-group mb-3">
                    <label asp-for="Name" class="form-label">Име на нивото на умение</label>
                    <input asp-for="Name" class="form-control" placeholder="Въведете име на нивото на умение" />
                    <span asp-validation-for="Name" class="text-danger"></span>
                </div>
            </div>
        </div>

        <div class="form-actions">
            <button type="submit" class="btn btn-primary">
                <i class="fas fa-save me-2"></i>Запази
            </button>
            <a asp-action="Index" class="btn btn-outline-secondary">
                <i class="fas fa-times me-2"></i>Отказ
            </a>
        </div>
    </form>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
