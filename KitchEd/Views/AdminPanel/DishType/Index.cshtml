@model IEnumerable<DishTypeViewModel>

@{
    ViewData["Title"] = "Dish Types";
}

<div class="container mt-4">
    <h2 class="mb-4">Dish Types</h2>

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success">@TempData["SuccessMessage"]</div>
    }
    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger">@TempData["ErrorMessage"]</div>
    }

    <a asp-action="Create" class="btn btn-primary mb-3">Create New Dish Type</a>

    <table class="table table-bordered">
        <thead class="thead-dark">
            <tr>
                <th>#</th>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var dishType in Model)
            {
                <tr>
                    <td>@dishType.DishTypeId</td>
                    <td>@dishType.Name</td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@dishType.DishTypeId" class="btn btn-warning btn-sm">Edit</a>
                        <button class="btn btn-danger btn-sm" onclick="confirmDelete(@dishType.DishTypeId)">Delete</button>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

<script>
    function confirmDelete(id) {
        if (confirm("Are you sure you want to delete this dish type?")) {
            window.location.href = "/DishType/Delete/" + id;
        }
    }
</script>
